version: 2
jobs:
  build:
    docker:
      - image: jtredoux/node-aws:latest

    working_directory: ~/cra-ci-demo

    steps:
      - checkout
      
      - run: 
        - npm install
        - npm run build

      - persist_to_workspace:
        root: .
        paths:
 

